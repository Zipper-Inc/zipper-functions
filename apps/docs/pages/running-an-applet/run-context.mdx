# Run Context

Within the `HandlerContext` [context object](/building-applets/handler-functions#the-context-object) in your handler function, you have access to the `request` object and the `runId`, both of which give you additional information about the specific run of the applet.
