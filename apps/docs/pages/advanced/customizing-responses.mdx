# Customizing Responses

The response object that your applet sends with its output can be customized by modifying the `response` object within the `HandlerContext` [context object](/building-applets/handler-functions#the-context-object) in your handler function.
