{
  "name": "deno-types",
  "description": "A safe place to put Deno types of the editor. Update with `bun run generate`.",
  "private": true,
  "version": "1.28.3",
  "scripts": {
    "generate:version": "DENO_VER=$(deno -V); DENO_VER=${DENO_VER/deno /}; sed -i '' -E 's/\"version\": \"(.*)\"/\"version\": \"'\"$DENO_VER\"'\"/g' package.json",
    "generate:file": "echo '// @ts-nocheck \n// generated with `bun run generate`\n// 🛑 DO NOT MODIFY\n\n'\"$(deno types)\" > generated/deno.d.ts",
    "generate": "run-s generate:*; echo '🦕 Updated deno.d.ts and package.json'; "
  }
}
