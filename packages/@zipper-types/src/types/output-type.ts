export enum OutputType {
  String = 'string',
  Object = 'object',
  Array = 'array',
  Collection = 'collection',
  Html = 'html',
  Action = 'action',
  ActionArray = 'action_array',
  Router = 'router',
}
